import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
  input: {
    display: "none",
  },
  star: {
    cursor: "pointer",
    transition: "color 200ms",
  },
}));
