import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
  searchBar: {
    borderRadius: 4,
    marginBottom: "1rem",
    display: "flex",
    padding: "16px",
  },
}));
